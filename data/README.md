# Candidate data pack

Place the six supplied PDFs and `ParcelPilot_Assessment_Data.xlsx` in `data/raw/`.
The raw directory is intentionally excluded from Git. The implementation validates exact
filenames and file checksums during ingestion.

